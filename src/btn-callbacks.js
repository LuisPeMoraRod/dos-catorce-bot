const mongoose = require("mongoose");
const { ToDo } = require("./model/ToDo");

const sendRes = require("./send-response");

const toDosMenu = async (chatID) => {};

const fetchToDos = async (chatID) => {};

const createToDo = async (chatID) => {};
